services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: diagramcode
      POSTGRES_PASSWORD: diagramcode
      POSTGRES_DB: diagramcode
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg2://diagramcode:diagramcode@postgres:5432/diagramcode
      REDIS_URL: redis://redis:6379/0
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app

  worker:
    build: ./backend
    env_file: ./backend/.env
    environment:
      DATABASE_URL: postgresql+psycopg2://diagramcode:diagramcode@postgres:5432/diagramcode
      REDIS_URL: redis://redis:6379/0
    command: ["rq", "worker", "-u", "redis://redis:6379/0"]
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app

volumes:
  postgres_data:
